# Own the Gym corpus (generate-and-review)

Ready-made B1–B2 German (DW, Nachrichtenleicht, Slow German, exam PDFs) cannot be stored if Davio might later charge. v1 stores only texts written for Davio (a model may draft). Third-party news stays a link-out companion, not the database. That keeps the hobby-to-product path and gives Noticing real syntax instead of flattened Einfache Sprache.

**Authoring (2026-08-24).** Seed facts and register from real pages; the sitting body is still owned. Write the scene first, then attach a catalog Pattern that occurs. Agents ship gym-copy quality on the first write. The founder is not the language-review queue. Recipe: `.agents/skills/gym-copy/`. Live piles are internal test text, not a public-beta corpus.
